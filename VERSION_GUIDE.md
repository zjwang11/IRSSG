# 版本号管理指南

## 📋 当前版本配置

### 包版本
- **当前版本**: `1.0.0`
- **版本类型**: 首次稳定发布

### 依赖版本范围
```toml
dependencies = [
    "numpy>=1.20.0,<3.0",           # 数值计算
    "scipy>=1.7.0,<2.0",            # 科学计算
    "pymatgen>=2022.0.0,<2026.0.0", # 材料科学
    "spglib>=1.16.0,<3.0",          # 对称性分析
]
```

### Python版本要求
- **最低版本**: Python 3.8
- **当前测试**: Python 3.12.2

## 🎯 版本号确定原则

### 1. 语义化版本控制 (SemVer)
```
主版本号.次版本号.修订号 (MAJOR.MINOR.PATCH)
```

### 2. 版本号含义
- **主版本号 (MAJOR)**: 破坏性更改，不向后兼容
- **次版本号 (MINOR)**: 新功能，向后兼容
- **修订号 (PATCH)**: Bug修复，向后兼容

### 3. 版本演进示例
```
1.0.0 → 1.0.1  # 修复bug
1.0.0 → 1.1.0  # 添加新功能
1.0.0 → 2.0.0  # 破坏性更改
```

## 🔍 依赖版本范围确定方法

### 1. 最低版本 (>=)
基于实际使用的功能确定：
- `numpy>=1.20.0`: 使用 `numpy.linalg.norm` 等函数
- `scipy>=1.7.0`: 使用 `scipy.linalg.null_space`
- `pymatgen>=2022.0.0`: 使用 `SpacegroupAnalyzer`
- `spglib>=1.16.0`: 使用 `find_primitive` 等函数

### 2. 最高版本 (<)
基于兼容性测试确定：
- `numpy<3.0`: 避免numpy 3.x可能的破坏性更改
- `scipy<2.0`: 避免scipy 2.x可能的破坏性更改
- `pymatgen<2026.0.0`: 基于pymatgen的发布周期
- `spglib<3.0`: 避免spglib 3.x可能的破坏性更改

## 🚀 版本发布流程

### 1. 发布前检查
```bash
# 测试兼容性
python version_recommender.py

# 检查依赖
pip check

# 运行测试
python -m pytest tests/
```

### 2. 版本号更新
```bash
# 更新pyproject.toml中的version
# 1.0.0 → 1.0.1 (修复版本)
# 1.0.0 → 1.1.0 (功能版本)
# 1.0.0 → 2.0.0 (主版本)
```

### 3. 重新构建和发布
```bash
# 清理旧版本
rm -rf build/ dist/ *.egg-info/

# 构建新版本
python -m build

# 安装测试
pip install dist/pos2ssg-1.0.1-py3-none-any.whl

# 发布到PyPI (可选)
twine upload dist/*
```

## 📊 版本历史记录

### 1.0.0 (当前版本)
- ✅ 首次稳定发布
- ✅ 支持POSCAR到SSG转换
- ✅ 完整的命令行界面
- ✅ 详细的文档和示例

### 计划中的版本

#### 1.0.1 (修复版本)
- 修复发现的bug
- 改进错误处理
- 更新文档

#### 1.1.0 (功能版本)
- 添加更多输出格式
- 支持更多文件类型
- 性能优化

#### 1.2.0 (功能版本)
- 添加图形界面
- 批量处理功能
- 更多配置选项

#### 2.0.0 (主版本)
- 重大架构更改
- API重构
- 破坏性更改

## 🛠️ 版本管理工具

### 1. 版本分析工具
```bash
# 分析当前版本状态
python version_analyzer.py

# 获取精确建议
python version_recommender.py
```

### 2. 依赖检查工具
```bash
# 检查依赖兼容性
pip check

# 查看依赖树
pipdeptree

# 检查过时的包
pip list --outdated
```

### 3. 版本比较工具
```bash
# 比较包版本
pip show package_name

# 检查版本兼容性
pip install package_name==specific_version
```

## 💡 最佳实践

### 1. 版本号选择
- 从0.1.0开始开发版本
- 1.0.0作为第一个稳定版本
- 根据更改类型选择版本号

### 2. 依赖管理
- 使用版本范围而不是固定版本
- 定期更新依赖版本
- 测试新版本的兼容性

### 3. 发布策略
- 修复版本快速发布
- 功能版本充分测试
- 主版本谨慎发布

### 4. 文档更新
- 每个版本更新CHANGELOG
- 保持README最新
- 记录破坏性更改

## 🔧 自动化版本管理

### 1. 使用bump2version
```bash
# 安装
pip install bump2version

# 配置.bumpversion.cfg
[bumpversion]
current_version = 1.0.0
commit = True
tag = True

[bumpversion:file:pyproject.toml]
search = version = "{current_version}"
replace = version = "{new_version}"
```

### 2. 使用Git标签
```bash
# 创建版本标签
git tag -a v1.0.0 -m "Release version 1.0.0"

# 推送标签
git push origin v1.0.0
```

### 3. 使用GitHub Actions
```yaml
# .github/workflows/release.yml
name: Release
on:
  push:
    tags:
      - 'v*'
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build package
        run: python -m build
      - name: Upload to PyPI
        run: twine upload dist/*
```

## 📝 总结

您的当前版本配置是合理的：
- ✅ 版本号1.0.0适合首次稳定发布
- ✅ 依赖版本范围设置合理
- ✅ Python版本要求适当
- ✅ 所有依赖兼容性良好

建议保持当前配置，根据后续的bug修复和功能添加来更新版本号。

